apiVersion: v1
kind: Pod

metadata:
  name: k8s-demo-pod
  labels:
    app: k8s-demo

spec:
  containers:
  - name: k8s-demo-container
    image: kubernetes_ass:v2
    imagePullPolicy: Never

    ports:
    - containerPort: 3000
